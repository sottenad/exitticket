
<% if teacher_signed_in? %>
  <p>
  <%= link_to('Logout', destroy_teacher_session_path, :method => :delete) %>        
  </p>
<% else %>
  <p>
  <%= link_to('Login as teacher', new_teacher_session_path)  %>  
  </p>
<% end %>

